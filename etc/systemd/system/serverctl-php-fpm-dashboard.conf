# Allow the privileged Dashboard actions invoked from PHP-FPM to update
# serverctl and Nginx configuration while ProtectSystem=full remains enabled.
[Service]
ReadWritePaths=/etc/serverctl /etc/nginx/sites-available
