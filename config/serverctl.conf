# serverctl production defaults
DEFAULT_PHP_VERSION=8.3
ALLOWED_PHP_VERSIONS=8.2 8.3 8.4
BACKUP_RETENTION=30
BACKUP_PATH=/var/backups/serverctl
WEB_ROOT=/var/www
PANEL_PORT=none
SSH_PORT=22
TIMEZONE=Asia/Bangkok
# Mounted NAS/NFS/SMB path or user@host:/absolute/path for rsync over SSH.
# REMOTE_BACKUP_TARGET=user@backup.example:/srv/server01
